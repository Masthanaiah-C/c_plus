#include <iostream>
#include <vector>
using namespace std;

int main()
{
	//this programs is about the data structures and its implementations
	/*
	 * STL standard template library
	 * algorithms -> sort
	 * containers -> array stack queue
	 * iterators -> how to access the members of the containers
	 * Functions -> sum 
	 */
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	vector<int> first_vector;
	int intake;
	cin>>intake;
	first_vector.push_back(intake);
	return 0;
}

